# 🚀 Деплой на Cloudflare Pages

## Подготовка (уже сделано)
✅ Удален мусор (.DS_Store, __MACOSX)
✅ Исправлен `base: '/'` в vite.config.js
✅ Создан `public/_redirects` для SPA
✅ Telegram токен перенесен на сервер (Cloudflare Function)

---

## 📦 1. Сборка проекта

```bash
npm run build
```

Это создаст папку `dist/` с готовым сайтом.

---

## 🌐 2. Деплой через Wrangler CLI

### Установите Wrangler (если еще не установлен):
```bash
npm install -g wrangler
```

### Залогиньтесь в Cloudflare:
```bash
npx wrangler login
```

### Задеплойте проект:
```bash
npx wrangler pages deploy dist
```

При первом деплое Wrangler спросит название проекта - введите `claro-landing` (или любое другое).

---

## 🔐 3. Настройка переменных окружения (ВАЖНО!)

После деплоя нужно добавить Telegram токены в Cloudflare Dashboard:

1. Откройте https://dash.cloudflare.com
2. Перейдите в **Workers & Pages** → найдите свой проект
3. Откройте вкладку **Settings** → **Environment variables**
4. Добавьте 2 переменные:

### Переменная 1:
- **Name**: `TELEGRAM_BOT_TOKEN`
- **Value**: `8527435735:AAFNRFeRJUzcR7Wk3FEhumzdJGw1bCg6vzo`
- **Environment**: Production

### Переменная 2:
- **Name**: `TELEGRAM_CHAT_ID`
- **Value**: `709195195`
- **Environment**: Production

5. Нажмите **Save**

**⚠️ БЕЗ ЭТИХ ПЕРЕМЕННЫХ ФОРМЫ НЕ БУДУТ РАБОТАТЬ!**

---

## ✅ 4. Проверка

После деплоя:

1. Откройте главную страницу
2. Откройте `/privacy-policy` напрямую (должна работать)
3. Откройте любую `/category/...` (должна работать)
4. **Протестируйте форму** - заполните бриф и отправьте (должно прийти в Telegram)

Если при прямом открытии `/privacy-policy` выдает 404 → проверьте что `_redirects` файл попал в `dist/`

---

## 🔄 Обновление сайта

При любых изменениях:

```bash
npm run build
npx wrangler pages deploy dist
```

---

## 🌍 Подключение домена

1. В Cloudflare Dashboard → ваш проект → **Custom domains**
2. Нажмите **Set up a custom domain**
3. Введите ваш домен (например, `claroplus.md`)
4. Следуйте инструкциям Cloudflare

Cloudflare автоматически настроит SSL и DNS.

---

## 📝 Структура проекта для Cloudflare

```
claro-landing/
├── functions/              # Serverless функции
│   └── send-telegram.js   # Telegram API (токен защищен)
├── public/
│   └── _redirects         # SPA routing
├── src/
│   └── utils/
│       └── telegram.js    # Вызывает функцию (без токена)
└── dist/                  # Собранный сайт (деплоится)
```

---

## ⚠️ Важно

- **Токены** теперь в переменных окружения Cloudflare (безопасно)
- **node_modules/** и **dist/** в .gitignore (не пушатся в Git)
- Cloudflare сам соберет проект при деплое из Git, НО через CLI деплоим уже собранный dist/

---

## 🆘 Проблемы?

### 404 на /privacy или /category/*
→ Проверьте что `_redirects` файл в `public/` и попал в `dist/`

### Формы не работают
→ Проверьте переменные окружения в Cloudflare Dashboard

### Изменения не применяются
→ Очистите кеш Cloudflare: Dashboard → Caching → Purge Cache
