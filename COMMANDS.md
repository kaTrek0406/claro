# 📝 Полезные команды

## 🚀 Разработка

```bash
# Установка зависимостей (первый раз)
npm install

# Запуск dev сервера
npm run dev
# Сайт будет доступен на http://localhost:5173

# Остановка dev сервера
# Нажмите Ctrl + C в терминале
```

## 🏗️ Сборка

```bash
# Сборка для продакшена
npm run build

# Предпросмотр prod сборки локально
npm run preview
# Откроется на http://localhost:4173

# Очистка кэша (если что-то работает странно)
rm -rf node_modules dist
npm install
npm run build
```

## 🔄 Git

```bash
# Инициализация (только первый раз)
git init
git add .
git commit -m "Initial commit"

# Добавление remote (замените на ваш URL)
git remote add origin https://github.com/USERNAME/REPO.git

# Первый push
git branch -M main
git push -u origin main

# Последующие коммиты
git add .
git commit -m "Описание изменений"
git push

# Проверка статуса
git status

# История коммитов
git log --oneline

# Откат последнего коммита (не запушенного)
git reset --soft HEAD~1
```

## 📦 GitHub Pages

```bash
# После первого push в main, деплой происходит автоматически
# Проверьте статус:
# GitHub → Ваш репозиторий → Actions

# URL вашего сайта:
# https://USERNAME.github.io/REPO_NAME/
```

## 🛠️ Разное

```bash
# Проверка версии Node.js
node -v

# Проверка версии npm
npm -v

# Обновление зависимостей (осторожно!)
npm update

# Проверка устаревших пакетов
npm outdated

# Поиск по коду
grep -r "текст для поиска" src/

# Список всех файлов в src
find src/ -type f
```

## 🐛 Отладка

```bash
# Если dev сервер не запускается:
# 1. Убедитесь, что порт 5173 свободен
lsof -ti:5173 | xargs kill -9  # MacOS/Linux
# или просто перезапустите терминал

# 2. Удалите node_modules и переустановите
rm -rf node_modules package-lock.json
npm install

# Если сборка падает с ошибкой:
# 1. Проверьте логи
npm run build --verbose

# 2. Очистите кэш
npm cache clean --force
rm -rf node_modules dist
npm install
```

## 🌍 Переменные окружения

```bash
# Для локальной разработки (автоматически)
NODE_ENV=development npm run dev

# Для продакшена (автоматически при сборке)
NODE_ENV=production npm run build
```

## 📊 Полезные проверки перед деплоем

```bash
# 1. Проверьте, что все файлы добавлены в git
git status

# 2. Соберите проект
npm run build

# 3. Проверьте размер сборки
du -sh dist/

# 4. Запустите предпросмотр
npm run preview

# 5. Откройте http://localhost:4173 и проверьте:
# - Логотип
# - Изображения
# - Навигация
# - Формы
# - Переключение языков
```