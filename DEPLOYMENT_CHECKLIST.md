# 🚀 Чек-лист деплоя на GitHub Pages

## ✅ Что уже сделано:
- [x] Создан GitHub Actions workflow (`.github/workflows/deploy.yml`)
- [x] Настроен `vite.config.js` с правильной базой
- [x] Создан `.gitignore` файл

## 📝 Что нужно сделать:

### 1. Проверьте название репозитория
Если вы хотите использовать другое название (не `claro-landing`), обновите `vite.config.js`:
```js
base: process.env.NODE_ENV === 'production' ? '/ваше-название-репо/' : '/',
```

### 2. Команды для деплоя:
```bash
# 1. Инициализируйте Git (если еще не сделали)
git init
git add .
git commit -m "Initial commit: Claro Digital Agency Landing"

# 2. Создайте репозиторий на GitHub и добавьте remote
git remote add origin https://github.com/ВАШ_USERNAME/claro-landing.git

# 3. Создайте main ветку и запушьте
git branch -M main
git push -u origin main
```

### 3. Настройте GitHub Pages:
1. Откройте Settings → Pages
2. Source: выберите **GitHub Actions**
3. Сохраните

### 4. Готово! 🎉
Ваш сайт будет доступен по адресу:
```
https://ВАШ_USERNAME.github.io/claro-landing/
```

## 🔄 Для обновления сайта:
```bash
git add .
git commit -m "Описание изменений"
git push
```
Деплой произойдет автоматически через 1-2 минуты!

## 🛠️ Локальная разработка:
```bash
npm install    # Первая установка
npm run dev    # Запуск dev сервера (http://localhost:5173)
npm run build  # Сборка для продакшена
npm run preview # Предпросмотр prod сборки
```

## ❗ Важные заметки:
- Первый деплой может занять 2-3 минуты
- Проверьте статус в разделе Actions вашего репозитория
- Убедитесь, что все изображения и файлы находятся в папке `public/`
- После изменения `vite.config.js` нужно пересобрать проект