# ✅ Финальная проверка перед деплоем

## 1. Проверка файлов
- [x] `.github/workflows/deploy.yml` - создан
- [x] `vite.config.js` - настроен с базой `/claro-landing/`
- [x] `.gitignore` - присутствует
- [x] `logo-claro.png` - перемещен в `public/`
- [x] `samolet.png` - перемещен в `public/`

## 2. Проверка сборки локально
Перед деплоем **обязательно** проверьте сборку:

```bash
# Установите зависимости (если еще не сделали)
npm install

# Соберите проект
npm run build

# Проверьте сборку локально
npm run preview
```

Откройте http://localhost:4173 и убедитесь, что:
- ✅ Логотип отображается
- ✅ Все изображения загружаются
- ✅ Навигация работает
- ✅ Форма отправки работает
- ✅ Переключение языков работает (RU/RO)
- ✅ Все анимации работают корректно

## 3. Важные заметки

### Если вы используете другое название репозитория:
Обновите строку в `vite.config.js`:
```js
base: process.env.NODE_ENV === 'production' ? '/ВАШЕ_НАЗВАНИЕ/' : '/',
```

### Если ваш репозиторий называется `username.github.io`:
Используйте:
```js
base: '/',
```

## 4. Команды для деплоя

### Первый деплой:
```bash
# 1. Инициализация Git
git init
git add .
git commit -m "Initial commit: Claro Landing Page"

# 2. Добавление remote (замените USERNAME и REPO_NAME)
git remote add origin https://github.com/USERNAME/REPO_NAME.git

# 3. Push
git branch -M main
git push -u origin main
```

### Последующие обновления:
```bash
git add .
git commit -m "Update: описание изменений"
git push
```

## 5. Настройка GitHub Pages
1. Перейдите в ваш репозиторий на GitHub
2. Settings → Pages
3. Source: выберите **GitHub Actions**
4. Подождите 1-2 минуты

## 6. Проверка деплоя
1. Откройте раздел **Actions** в репозитории
2. Дождитесь завершения workflow "Deploy to GitHub Pages"
3. Если есть ошибки - проверьте логи
4. После успешного завершения откройте сайт:
   ```
   https://USERNAME.github.io/REPO_NAME/
   ```

## 7. Если что-то не работает

### Логотип или изображения не отображаются:
- Проверьте, что все файлы в папке `public/`
- Проверьте правильность путей (должны быть `/logo-claro.png`, не `./logo-claro.png`)

### 404 ошибка при навигации:
- Убедитесь, что `base` в `vite.config.js` соответствует названию репозитория
- Пересоберите проект: `npm run build`

### GitHub Actions падает с ошибкой:
- Проверьте, что в Settings → Pages выбран источник "GitHub Actions"
- Убедитесь, что у Actions есть права на запись (Settings → Actions → General → Workflow permissions)

## 8. Готово! 🎉
После успешного деплоя поделитесь ссылкой с клиентами!